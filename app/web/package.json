{
    "name": "web",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "build": "rsbuild build",
        "dev": "rsbuild dev --open",
        "preview": "rsbuild preview",
        "format": "prettier --check .",
        "test": "echo test",
        "lint": "eslint .",
        "format:write": "prettier --write ."
    },
    "dependencies": {
        "@feature/config": "workspace:*",
        "solid-js": "^1.8.18"
    },
    "devDependencies": {
        "@rsbuild/core": "1.0.0-alpha.4",
        "@rsbuild/plugin-babel": "1.0.0-alpha.4",
        "@rsbuild/plugin-solid": "1.0.0-alpha.4",
        "@rsbuild/plugin-type-check": "1.0.0-alpha.4"
    }
}
